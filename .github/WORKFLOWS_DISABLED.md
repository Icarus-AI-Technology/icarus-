# Desabilitar workflows CI temporariamente
# Estes workflows requerem secrets e configuração adicional
# Para reativar, remova este arquivo e restaure os workflows

# Workflows desabilitados:
# - a11y-tests.yml (Accessibility Tests)
# - qa-ocr.yml (QA OCR Benchmarks)
# - storybook.yml (Storybook CI)
# - qa.yml (QA Audits)

# Para deploy, use apenas o Vercel CLI ou o dashboard:
# vercel --prod

